const activo = true;

let mensaje = "";

const activo = true;

if (!activo) {
    mensaje = "Activo";
} else {
    mensaje = "Inactivo";
}

const mensaje = (activo) ? "Activo" : "Inactivo";
const mensaje = (activo) ? "Activo" : null;

const mensaje = activo && "Activo";